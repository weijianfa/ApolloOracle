# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_tooken

# PingPong Payment Configuration
PINGPONG_API_KEY=your_pingpong_api_key_here
PINGPONG_WEBHOOK_SECRET=your_webhook_secret_here
PINGPONG_MERCHANT_ID=your_merchant_id_here
# 设置为true即可使用Mock模式（开发环境推荐）
PINGPONG_USE_MOCK=true
PINGPONG_MOCK_PAYMENT_URL=https://example.com/mock-pingpong-payment

# DeepSeek API Configuration
DEEPSEEK_API_KEY=sk-bef05d8bbab94a4a94dcbac1907e1409
DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions

# DeepSeek API Timeout Settings (in seconds)
# Single request timeout: default 60 seconds (1 minute)
# AI generation time depends on content size, especially when deep thinking is enabled
# Increase this value if you encounter frequent timeout errors
DEEPSEEK_API_TIMEOUT=120

# Total timeout including retries: default 240 seconds (4 minutes)
# Calculation: 60s * 3 attempts + retry intervals (1s + 2s) 鈮?183s, set to 240s for safety
# Increase this value if you need longer generation time
DEEPSEEK_API_TOTAL_TIMEOUT=240

# Yuanfenju API Configuration (Bazi API)
YUANFENJU_API_KEY=jNYT2zEpPXOwCBdod5iWPgczn
YUANFENJU_API_URL=https://api.yuanfenju.com/v1/bazi

# Exchange Rate API Configuration
EXCHANGE_RATE_API_KEY=your_exchange_rate_key
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest/USD

# Database Configuration (SQLite for development)
DATABASE_URL=sqlite:///bot.db
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://user:pass@host/db

# Redis Configuration (Optional, not required for development)
REDIS_URL=redis://localhost:6379/0

# Admin Configuration
ADMIN_TELEGRAM_ID=your_admin_id
ADMIN_EMAIL=admin@example.com

# Environment
ENVIRONMENT=development  # development or production

# Log Level
LOG_LEVEL=INFO

# MaxMind GeoIP Configuration (Optional)
MAXMIND_LICENSE_KEY=your_maxmind_license_key_here

# Webhook Configuration (Production environment)
WEBHOOK_URL=https://your-domain.com/webhook
WEBHOOK_PORT=8443
WEBHOOK_LISTEN=0.0.0.0

# SSL Certificate Path (Production environment)
SSL_CERT_PATH=/path/to/cert.pem
SSL_KEY_PATH=/path/to/key.pem
